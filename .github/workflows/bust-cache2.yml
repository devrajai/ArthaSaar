name: bust-app-cache-2
on: workflow_dispatch
permissions:
  contents: write
jobs:
  bust:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: bump version on app.js script tag
        run: |
          python3 - << 'EOF'
          s = open("index.html", encoding="utf-8").read()
          old = '<script src="app.js?v=21sep26a"></script>'
          new = '<script src="app.js?v=21sep26b"></script>'
          assert s.count(old) == 1, "old tag not found exactly once"
          open("index.html", "w", encoding="utf-8").write(s.replace(old, new))
          print("version bumped")
          EOF
      - name: commit and push
        run: |
          git config user.name "devrajai"
          git config user.email "devraj.resume@gmail.com"
          git add index.html
          git commit -m "chore: bump app.js version tag (bluefix2)" || echo "nothing to commit"
          for i in 1 2 3; do git push && break; git pull --rebase -X theirs || true; done
